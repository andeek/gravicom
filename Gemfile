source 'https://rubygems.org'

gem 'jekyll'

# If they accept my two pull requests, adding support for
# twbs and font-awesome, we can use the original library
gem 'jekyll-assets', github: 'mcorp/jekyll-assets'

gem 'sass'
gem 'bootstrap-sass'
gem 'font-awesome-sass'
